---
description: "Remove the Xcode DerivedData directory belonging to a worktree."
---

- Remove the derived data directory for the worktree. Xcode names DerivedData project directories as `{ProjectName}-{hash}`, where the hash is a 28-character base-26 encoding of the MD5 digest of the absolute path to the .xcodeproj or .xcworkspace file.

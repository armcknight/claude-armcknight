---
description: "Review, then stage and commit the work in conventional commit format."
---

First, perform /claude-armcknight:review

Then stage the relevant code in git and create a commit describing the changes. Use the [conventional commit](https://www.conventionalcommits.org/en/v1.0.0/) format, scoping to specific components when possible like `fix(AutofixStatusView): <message>` or `feat(seer setup): <message>`.
